# Database — update host/port/credentials to match your environment
# Docker setup:  use localhost:5433 (host port mapped in docker-compose.yml)
# Local setup:   use localhost:5432
DATABASE_URL="postgresql://gotyolo:gotyolo@localhost:5433/gotyolo?schema=public"

# HTTP server port
PORT=3000

# Environment: development | production
NODE_ENV=development
